# M2 Valid Artifact Fixture

This directory is a schema fixture for the M2 artifact contract.

It is intentionally tiny and is not the output of real ONNX quantization. It exists so CI, bridge code, and Runtime code can validate the artifact shape before real quantization is implemented.
